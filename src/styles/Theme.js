import { Dimensions } from "react-native";

export const colors = {
    color_blue: "#1976d2",
    color_light_blue: "#dcebfb",
    color_light_gray: "#E0E0E0",
    color_gray: "#61677A",
    color_white: "#fff",
};

const { width, height } = Dimensions.get("window");

export const sizes = {
    width,
    height,
};
